version: '3.8'

services:
  nginx-host:
    image: nginx:alpine
    container_name: mw-panel-nginx-host
    network_mode: host
    volumes:
      - ./dist-frontend:/usr/share/nginx/html
      - ./nginx-host.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl
    restart: unless-stopped